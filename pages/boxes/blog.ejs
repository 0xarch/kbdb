<CBox>
    <ibox>
        <ilabel><%= item.title %></ilabel>
        <small>SID <%= item.sid %></small>
    </ibox>
    <hr>
    <small>作者：<%= item.author %></small>
    <hr>
    <Tx><%= item.description %></Tx>
    <Action onclick="go('<%= item.time %>/<%= item.sid %><%= item.title %>/')">阅读</Action>
</CBox>